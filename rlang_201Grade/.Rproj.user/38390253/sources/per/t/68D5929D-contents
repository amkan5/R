
cal <- function(op,num1,num2){
  return(switch(op,
                "+" = num1+num2,
                "-" = num1-num2,
                "/" = num1%/%num2,
                "*" = num1*num2,
                "%" = num1%%num2))
}

res<-cal("+",3,5)
cat(res)

